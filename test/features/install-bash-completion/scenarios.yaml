defaults:
  options:
    log_level: trace

apt_default:
  envs: [debian-latest]
  modes: [devcontainer]
  tests: [apt_default]

apt_disabled:
  envs: [debian-latest]
  modes: [devcontainer]
  options:
    install_bash_completion: false
  tests: [apt_disabled]
