IMAGE=debian:latest
SETUP_CMD="cat > /usr/local/bin/jq <<'EOF'
#!/usr/bin/env bash
if [[ \"\${1-}\" == \"--version\" ]]; then echo \"jq-9.9.9 (fake)\"; exit 0; fi
exit 0
EOF
chmod +x /usr/local/bin/jq"
