IMAGE=debian:latest
SETUP_CMD="cat > /usr/local/bin/shfmt <<'EOF'
#!/usr/bin/env bash
if [[ \"\${1-}\" == \"--version\" ]]; then echo \"v3.0.0 (fake)\"; exit 0; fi
exec cat
EOF
chmod +x /usr/local/bin/shfmt"
