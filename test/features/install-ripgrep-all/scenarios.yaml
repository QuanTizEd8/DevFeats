default_install:
  envs: [ubuntu-latest+ripgrep]
  modes: [devcontainer, standalone]
  tests: [default_install]

package_install:
  envs: [arch+bash]
  modes: [devcontainer, standalone]
  options:
    method: package
  tests: [package_install]

install_adapters:
  envs: [ubuntu-latest+ripgrep]
  modes: [standalone]
  options:
    install_adapters: true
  tests: [install_adapters]

if_exists_fail:
  expect_install_failure: true
  envs: [ubuntu-latest]
  modes: [standalone]
  setup: |
    printf '#!/usr/bin/env bash\nif [ "${1-}" = "--version" ]; then printf "ripgrep-all 99.99.99\n"; exit 0; fi\nexit 0' \
      > /usr/local/bin/rga
    chmod +x /usr/local/bin/rga
  options:
    if_exists: fail
  tests: [if_exists_fail]

if_exists_skip:
  envs: [ubuntu-latest]
  modes: [standalone]
  setup: |
    printf '#!/usr/bin/env bash\nif [ "${1-}" = "--version" ]; then printf "ripgrep-all 99.99.99\n"; exit 0; fi\nexit 0' \
      > /usr/local/bin/rga
    chmod +x /usr/local/bin/rga
  options:
    if_exists: skip
  tests: [if_exists_skip]
