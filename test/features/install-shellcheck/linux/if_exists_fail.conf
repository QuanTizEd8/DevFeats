IMAGE=debian:latest
SETUP_CMD="cat > /usr/local/bin/shellcheck <<'EOF'
#!/usr/bin/env bash
if [[ \"\${1-}\" == \"--version\" ]]; then printf 'ShellCheck - shell script analysis tool\nversion: 0.0.0 (fake)\n'; exit 0; fi
exit 0
EOF
chmod +x /usr/local/bin/shellcheck"
