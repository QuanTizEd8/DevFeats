defaults:
  modes: [devcontainer]
  options:
    method: binary

# ── baseline ─────────────────────────────────────────────────────────────────

default_install:
  envs: [ubuntu-latest]
  tests: [default_install]

# ── new options ───────────────────────────────────────────────────────────────

copilot_home:
  envs: [ubuntu-latest]
  options:
    copilot_home: ~/copilot-test
  tests: [copilot_home]

copilot_model:
  envs: [ubuntu-latest]
  options:
    copilot_model: claude-sonnet-4-6
  tests: [copilot_model]

plugins:
  envs: [ubuntu-latest]
  options:
    plugins: workiq@copilot-plugins
  tests: [plugins]
