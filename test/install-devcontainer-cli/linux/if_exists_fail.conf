IMAGE=debian:latest
SETUP_CMD="cat > /usr/local/bin/devcontainer <<'EOF'
#!/usr/bin/env bash
if [[ \"\${1-}\" == \"--version\" ]]; then echo \"0.0.0\"; exit 0; fi
exit 0
EOF
chmod +x /usr/local/bin/devcontainer"
