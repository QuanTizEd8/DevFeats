ubuntu-stable:
  env: ubuntu-stable

debian-stable:
  env: debian-stable+bash

fedora-current:
  env: fedora-current+bash

rockylinux-current:
  env: rockylinux-current+bash

alpine-current:
  env: alpine-current+bash

opensuse-leap-current:
  env: opensuse-leap-current+bash

archlinux-current:
  env: archlinux-current
