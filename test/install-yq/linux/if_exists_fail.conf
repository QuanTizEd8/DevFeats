IMAGE=debian:latest
SETUP_CMD="cat > /usr/local/bin/yq <<'EOF'
#!/usr/bin/env bash
if [[ \"\${1-}\" == \"-o=json\" ]]; then exit 0; fi
if [[ \"\${1-}\" == \"--version\" ]]; then echo \"yq (fake)\"; exit 0; fi
exit 0
EOF
chmod +x /usr/local/bin/yq"
