IMAGE=debian:latest
SETUP_CMD=printf '#!/usr/bin/env bash\nif [[ "${1-}" == "-o=json" ]]; then exit 0; fi\nif [[ "${1-}" == "--version" ]]; then echo "yq (fake)"; exit 0; fi\nexit 0\n' > /usr/local/bin/yq && chmod +x /usr/local/bin/yq
