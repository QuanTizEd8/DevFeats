# Install dependencies/run/base.yaml when root (see argparse.py).
if users__is_root; then
  _run_deps__install_base
else
  logging__info "Skipping base run-dependency install (not root); ensure packages from dependencies/run/base.yaml are already installed."
fi
