packages:
  # ── HTTP / file transfer ──────────────────────────────────────────────────
  - curl
  - wget

  # ── Parallel multi-protocol downloader ───────────────────────────────────
  - aria2

  # ── Remote sync ───────────────────────────────────────────────────────────
  - rsync

  # ── Cryptography / key management ────────────────────────────────────────
  - name: gnupg
    dnf: gnupg2
    yum: gnupg2
    zypper: gpg2

  # ── TLS / certificate tooling ─────────────────────────────────────────────
  - openssl

  # ── Secure shell client ────────────────────────────────────────────────────
  - name: openssh-client
    brew: openssh
    dnf: openssh-clients
    yum: openssh-clients
    zypper: openssh
    pacman: openssh

  # ── DNS utilities (dig, nslookup, host) ───────────────────────────────────
  - name: dnsutils
    brew: bind
    apk: bind-tools
    dnf: bind-utils
    yum: bind-utils
    zypper: bind-utils
    pacman: bind

  # ── TCP/UDP connection testing ────────────────────────────────────────────
  - name: netcat-openbsd
    brew: netcat
    dnf: nmap-ncat
    yum: nmap-ncat
    zypper: ncat
    pacman: openbsd-netcat

  # ── Port scanner / host discovery ────────────────────────────────────────
  - nmap

  # ── ICMP ping (Linux-only — macOS ships ping as a system tool) ────────────
  - name: iputils
    when: {kernel: linux}
    apt: iputils-ping

  # ── Network diagnostics (traceroute + ping) ───────────────────────────────
  # mtr-tiny is the headless variant (no GTK) — preferred for containers
  - name: mtr
    apt: mtr-tiny

  # ── Multipurpose relay ────────────────────────────────────────────────────
  - socat

  # ── Bandwidth measurement ─────────────────────────────────────────────────
  - iperf3

  # ── Packet capture ────────────────────────────────────────────────────────
  - tcpdump

  # ── WHOIS domain lookups ─────────────────────────────────────────────────
  - whois

  # ── Human-friendly HTTP client ────────────────────────────────────────────
  - name: httpie
    pacman: python-httpie

  # ── Interactive TLS HTTP/HTTPS intercepting proxy ─────────────────────────
  - mitmproxy

  # ── Network interface and routing (Linux-only) ────────────────────────────
  - name: iproute2
    when: {kernel: linux}
    dnf: iproute
    yum: iproute
