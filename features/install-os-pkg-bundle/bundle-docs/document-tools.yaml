ghostscript:
  summary: Interpreter for PostScript and PDF.
  description: |
    Ghostscript renders, converts, and prints PostScript and PDF files. Used
    as a backend for PDF viewers and by document pipelines for EPS→PDF
    conversion and rasterisation.
  url:
    docs: https://www.ghostscript.com/doc/current/
    source: https://github.com/ArtifexSoftware/ghostpdl

poppler-utils:
  summary: PDF rendering library utilities (pdftotext, pdfimages, pdftoppm …).
  description: |
    Poppler is a PDF rendering library derived from Xpdf. The -utils package
    provides command-line tools: pdftotext (text extraction), pdfimages (image
    extraction), pdftoppm (rasterisation), pdfinfo (metadata), and more.
  url:
    docs: https://poppler.freedesktop.org/
    source: https://gitlab.freedesktop.org/poppler/poppler
  notes: |
    Package named 'poppler-utils' on apt, apk, dnf; 'poppler-tools' on
    zypper; 'poppler' on brew and pacman.

graphviz:
  summary: Graph visualization software.
  description: |
    Graphviz automatically generates diagrams from DOT-language textual
    descriptions. Used by Sphinx (dot directive), Doxygen, Jupyter notebooks
    with graphviz, and scientific papers that visualize graphs or workflows.
  url:
    docs: https://graphviz.org/documentation/
    source: https://gitlab.com/graphviz/graphviz

gnuplot-nox:
  summary: Headless command-line plotting program.
  description: |
    gnuplot creates 2D and 3D plots in formats including PostScript, PDF, PNG,
    and SVG. The -nox variant (apt only) excludes X11/Qt GUIs — all other PMs
    ship a single 'gnuplot' package. Used as a backend for pgfplots in TeX.
  url:
    docs: https://gnuplot.info/documentation.html

imagemagick:
  summary: Image manipulation tools.
  description: |
    ImageMagick creates, edits, composes, and converts bitmap images in many
    formats. Used by Sphinx (image conversion), Pandoc, and document pipelines.
    Note: on dnf (Fedora) and zypper (openSUSE) the package is 'ImageMagick'
    (capital I and M).
  url:
    docs: https://imagemagick.org/script/resources.php
    source: https://github.com/ImageMagick/ImageMagick

fontconfig:
  summary: Font configuration and customization library.
  description: |
    fontconfig provides system-wide font configuration and discovery.
    Required by document processing tools (LaTeX, Sphinx) and PDF renderers
    that embed or select fonts.
  url:
    docs: https://manpages.ubuntu.com/manpages/noble/en/man5/fonts-conf.5.html

python3-pygments:
  summary: Python syntax highlighting library.
  description: |
    Pygments is a generic syntax highlighter supporting hundreds of languages.
    Required by Sphinx (code-block directives), MkDocs, and LaTeX minted
    package for formatted code listings.
  url:
    docs: https://pygments.org/docs/
    source: https://github.com/pygments/pygments

librsvg2-bin:
  summary: SVG rendering and conversion utility (rsvg-convert).
  description: |
    librsvg2-bin provides rsvg-convert, a command-line tool for converting
    SVG files to PNG, PDF, PS, and other raster/vector formats. Used by
    document pipelines, icon build systems, and LaTeX workflows that include
    SVG artwork.
  url:
    docs: https://gnome.pages.gitlab.gnome.org/librsvg/
    source: https://gitlab.gnome.org/GNOME/librsvg
  notes: |
    Package named 'librsvg2-bin' on apt; 'librsvg' on apk and pacman;
    'librsvg2-tools' on dnf; 'rsvg-convert' on zypper; 'librsvg' on brew.

tesseract-ocr:
  summary: Open-source OCR engine.
  description: |
    Tesseract is the leading open-source OCR engine, originally developed at
    HP and now maintained by Google. Reads image files and outputs recognized
    text in formats including UTF-8, hOCR, PDF, and TSV. Language packs are
    installed separately (e.g. tesseract-ocr-eng for English).
  url:
    docs: https://tesseract-ocr.github.io/tessdoc/
    source: https://github.com/tesseract-ocr/tesseract
  notes: |
    Package named 'tesseract-ocr' on apt; 'tesseract' on brew, apk, dnf,
    zypper, and pacman.


