version: 0.1.0
name: Codex Installation
description: Install [Codex](https://openai.com/codex/) – OpenAI's coding agent.
keywords:
- codex
- ai
- llm
_long_description: >-
  Placeholder
_options:
  installer_dir: false
options:
  codex_home:
    type: string
    default: .codex
    description: |-
      Placeholder
onCreateCommand:
  symlink-codex-dir:
    description: Symlink the specified workspace `codex` directory to `~/.codex`.
    command: >-
      ${{ _env_vars.lifecycle_script_dir }}$/on-create--symlink-codex-dir.sh ${containerWorkspaceFolder}
customizations:
  vscode:
    extensions:
      - openai.chatgpt
