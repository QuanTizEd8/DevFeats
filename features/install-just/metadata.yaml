version: 0.1.0
name: just Installation
description: Install [just](https://just.systems/) via release assets, package repositories, the official script, or Cargo.
keywords:
  - just
  - task-runner
  - build
  - cli
_long_description: |-
  Installs the `just` command runner with configurable installation methods:
  GitHub release assets, distro package repositories, the official installer script,
  and Cargo-based installation.
options:
  script_force:
    type: boolean
    default: false
    description: Add `--force` when running the official installer script.
_options:
  verify:
    args: "--version"
    description: Verify just is installed and working.
  gh_repo: casey/just
  version:
    resolution: github_release
    default: stable
    inputs: [stable, latest, semver]
    proposals:
      - value: "1.50.0"
        description: Install just v1.50.0.
  method:
    binary:
      asset_uri: "https://github.com/${{ _options.gh_repo }}$/releases/download/{TAG}/just-{VERSION}-{RUST_TRIPLE}.tar.gz"
      binary_src: just
      sidecar_uri: "https://github.com/${{ _options.gh_repo }}$/releases/download/{TAG}/SHA256SUMS"
    package: {}
    script:
      asset_uri: "https://just.systems/install.sh"
    cargo:
      crate: just
  prefix:
    bins: [just]
    applies_when:
      - method: [binary, script, cargo]
customizations:
  vscode:
    extensions:
      - nefrob.vscode-just-syntax
_dependencies:
  run:
    base:
      packages:
        - ca-certificates
    os-pkg:
      packages:
        - just
# installsAfter: []
