[pytest]
cache_dir = .local/cache/pytest
# Default capture can hit "I/O operation on closed file" with jsonschema on some
# filesystems (e.g. external drives). Prefer `pixi run test-py` or pass --capture=no.
# addopts = --capture=no
