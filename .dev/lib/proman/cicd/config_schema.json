{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Single config object emitted by proman-cicd-detect and passed as a JSON string to every reusable workflow.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "cm_devcontainer",
    "ci_build",
    "ci_lint",
    "ci_test_dev",
    "ci_test_feat",
    "ci_test_lib",
    "cd"
  ],
  "properties": {
    "cm_devcontainer": {
      "description": "Config slice for the cm-devcontainer reusable workflow (multi-platform devcontainer image build and push).",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "enabled",
        "image_name",
        "image_tag",
        "tag_is_latest",
        "config_dir",
        "userdata_dir",
        "cache_ref_prefix",
        "registry",
        "build_matrix"
      ],
      "properties": {
        "enabled": {
          "description": "Whether to rebuild the devcontainer image.",
          "type": "boolean"
        },
        "image_name": {
          "description": "Full image name without tag, e.g. ghcr.io/org/repo-devcontainer.",
          "type": "string"
        },
        "image_tag": {
          "description": "Image tag to push, e.g. 'latest' or 'branch-my-feature'.",
          "type": "string"
        },
        "tag_is_latest": {
          "description": "True when image_tag == 'latest'; controls whether a sha-prefixed alias tag is also pushed.",
          "type": "boolean"
        },
        "config_dir": {
          "description": "Repo-relative path to the devcontainer config directory.",
          "type": "string"
        },
        "userdata_dir": {
          "description": "Absolute path used as the devcontainer userdata/cache folder on the runner.",
          "type": "string"
        },
        "cache_ref_prefix": {
          "description": "Registry-side tag prefix used for buildx layer-cache refs.",
          "type": "string"
        },
        "registry": {
          "description": "Container registry hostname used for docker/login-action.",
          "type": "string"
        },
        "build_matrix": {
          "description": "Per-platform build jobs; each entry maps to one GHA matrix runner.",
          "type": "array",
          "items": {
            "description": "A single platform build job.",
            "type": "object",
            "additionalProperties": false,
            "required": [
              "runner",
              "platform",
              "platform_tag"
            ],
            "properties": {
              "runner": {
                "description": "GHA runs-on label for this platform.",
                "type": "string"
              },
              "platform": {
                "description": "Docker platform string passed to buildx, e.g. linux/amd64.",
                "type": "string"
              },
              "platform_tag": {
                "description": "Short suffix appended to the image tag for the per-arch manifest, e.g. linux-amd64.",
                "type": "string"
              }
            }
          }
        }
      }
    },
    "ci_build": {
      "description": "Config slice for the ci-build reusable workflow (source packaging and docs build).",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "ci_image",
        "source",
        "docs"
      ],
      "properties": {
        "ci_image": {
          "description": "Full devcontainer image URI used as the job container.",
          "type": "string"
        },
        "source": {
          "description": "Controls the source-packaging job that produces the src and dist artifacts.",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "enabled",
            "artifact_src",
            "artifact_dist"
          ],
          "properties": {
            "enabled": {
              "description": "Whether to run the source-packaging job.",
              "type": "boolean"
            },
            "artifact_src": {
              "description": "Artifact spec for the raw source tree uploaded for downstream jobs.",
              "$ref": "#/$defs/artifact"
            },
            "artifact_dist": {
              "description": "Artifact spec for the distribution tarballs uploaded for release.",
              "$ref": "#/$defs/artifact"
            }
          }
        },
        "docs": {
          "description": "Controls the documentation build job.",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "enabled",
            "artifact"
          ],
          "properties": {
            "enabled": {
              "description": "Whether to run the docs build job.",
              "type": "boolean"
            },
            "artifact": {
              "description": "Artifact spec for the built docs archive uploaded for GitHub Pages deployment.",
              "$ref": "#/$defs/artifact"
            }
          }
        }
      }
    },
    "ci_lint": {
      "description": "Config slice for the ci-lint reusable workflow (shell, JSON schema, and Python linting).",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "ci_image",
        "artifact_src_name",
        "artifact_src_path",
        "shell",
        "validate",
        "python"
      ],
      "properties": {
        "ci_image": {
          "description": "Full devcontainer image URI used as the job container.",
          "type": "string"
        },
        "artifact_src_name": {
          "description": "Name of the source artifact downloaded by all lint jobs.",
          "type": "string"
        },
        "artifact_src_path": {
          "description": "Local path where the source artifact is extracted.",
          "type": "string"
        },
        "shell": {
          "description": "Controls the shell-script linting job (shellcheck / shfmt).",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "enabled"
          ],
          "properties": {
            "enabled": {
              "description": "Whether to run shell linting.",
              "type": "boolean"
            }
          }
        },
        "validate": {
          "description": "Controls the devcontainer-feature JSON schema validation job.",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "enabled",
            "features_path"
          ],
          "properties": {
            "enabled": {
              "description": "Whether to run JSON schema validation.",
              "type": "boolean"
            },
            "features_path": {
              "description": "Path passed to devcontainers/action base-path-to-features.",
              "type": "string"
            }
          }
        },
        "python": {
          "description": "Controls the Python linting job (ruff check + format).",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "enabled"
          ],
          "properties": {
            "enabled": {
              "description": "Whether to run Python linting.",
              "type": "boolean"
            }
          }
        }
      }
    },
    "ci_test_dev": {
      "description": "Config slice for the ci-test-dev reusable workflow (Python unit tests for proman dev tooling).",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "enabled",
        "ci_image"
      ],
      "properties": {
        "enabled": {
          "description": "Whether to run the Python dev tests.",
          "type": "boolean"
        },
        "ci_image": {
          "description": "Full devcontainer image URI used as the job container.",
          "type": "string"
        }
      }
    },
    "ci_test_feat": {
      "description": "Config slice for the ci-test-feat reusable workflow (devcontainer feature integration tests).",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "enabled",
        "feature_matrix"
      ],
      "properties": {
        "enabled": {
          "description": "Whether any feature tests should run; false when feature_matrix is empty.",
          "type": "boolean"
        },
        "feature_matrix": {
          "description": "Per-feature test configuration objects; each entry is passed as inputs.config to ci-test-feat.yaml.",
          "type": "array",
          "items": {
            "description": "Self-contained configuration for a single feature's test jobs across all platforms.",
            "type": "object",
            "additionalProperties": false,
            "required": [
              "feature",
              "artifact_src_name",
              "artifact_src_path",
              "ci_image",
              "registry",
              "free_disk_space",
              "devcontainer",
              "linux",
              "macos"
            ],
            "properties": {
              "feature": {
                "description": "Feature ID, e.g. install-pixi.",
                "type": "string"
              },
              "artifact_src_name": {
                "description": "Name of the source artifact downloaded by test jobs.",
                "type": "string"
              },
              "artifact_src_path": {
                "description": "Local path where the source artifact is extracted.",
                "type": "string"
              },
              "ci_image": {
                "description": "Full devcontainer image URI used as the DinD host container.",
                "type": "string"
              },
              "registry": {
                "description": "Container registry hostname used for docker/login-action.",
                "type": "string"
              },
              "free_disk_space": {
                "description": "Flags passed to jlumbroso/free-disk-space to reclaim runner disk before large Docker builds.",
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "tool_cache",
                  "swap_storage",
                  "docker_images",
                  "android",
                  "dotnet",
                  "haskell",
                  "large_packages"
                ],
                "properties": {
                  "tool_cache": {
                    "description": "Remove the GHA tool cache.",
                    "type": "boolean"
                  },
                  "swap_storage": {
                    "description": "Remove swap storage.",
                    "type": "boolean"
                  },
                  "docker_images": {
                    "description": "Remove pre-installed Docker images.",
                    "type": "boolean"
                  },
                  "android": {
                    "description": "Remove Android SDK.",
                    "type": "boolean"
                  },
                  "dotnet": {
                    "description": "Remove .NET SDK.",
                    "type": "boolean"
                  },
                  "haskell": {
                    "description": "Remove Haskell toolchain.",
                    "type": "boolean"
                  },
                  "large_packages": {
                    "description": "Remove large apt packages.",
                    "type": "boolean"
                  }
                }
              },
              "devcontainer": {
                "description": "devcontainer-mode scenarios for this feature.",
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "enabled",
                  "scenarios"
                ],
                "properties": {
                  "enabled": {
                    "description": "Whether any devcontainer-mode scenarios exist for this feature.",
                    "type": "boolean"
                  },
                  "scenarios": {
                    "description": "Scenario keys to run in devcontainer mode, one GHA job each.",
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  }
                }
              },
              "linux": {
                "description": "standalone-mode scenarios for this feature.",
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "enabled",
                  "scenarios"
                ],
                "properties": {
                  "enabled": {
                    "description": "Whether any standalone-mode scenarios exist for this feature.",
                    "type": "boolean"
                  },
                  "scenarios": {
                    "description": "Scenario keys to run in standalone mode, one GHA job each.",
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  }
                }
              },
              "macos": {
                "description": "macOS-mode scenarios for this feature.",
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "enabled",
                  "scenarios"
                ],
                "properties": {
                  "enabled": {
                    "description": "Whether any macOS scenarios exist for this feature.",
                    "type": "boolean"
                  },
                  "scenarios": {
                    "description": "macOS scenarios, one GHA job each on a fresh native runner.",
                    "type": "array",
                    "items": {
                      "description": "A single macOS scenario job.",
                      "type": "object",
                      "additionalProperties": false,
                      "required": [
                        "scenario",
                        "runner"
                      ],
                      "properties": {
                        "scenario": {
                          "description": "Scenario key to run.",
                          "type": "string"
                        },
                        "runner": {
                          "description": "GHA runs-on label for the macOS runner.",
                          "type": "string"
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "ci_test_lib": {
      "description": "Config slice for the ci-test-lib reusable workflow (shell library unit tests).",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "enabled",
        "ci_image",
        "artifact_src_name",
        "artifact_src_path",
        "linux_matrix",
        "macos_matrix"
      ],
      "properties": {
        "enabled": {
          "description": "Whether to run library unit tests.",
          "type": "boolean"
        },
        "ci_image": {
          "description": "Full devcontainer image URI used as the job container.",
          "type": "string"
        },
        "artifact_src_name": {
          "description": "Name of the source artifact downloaded by lib test jobs.",
          "type": "string"
        },
        "artifact_src_path": {
          "description": "Local path where the source artifact is extracted.",
          "type": "string"
        },
        "linux_matrix": {
          "description": "Linux distro environments to run unit tests against, sourced from test/lib/scenarios.yaml.",
          "type": "array",
          "items": {
            "description": "A single Linux unit test environment.",
            "type": "object",
            "additionalProperties": false,
            "required": [
              "name",
              "env"
            ],
            "properties": {
              "name": {
                "description": "Human-readable environment name used in the job display name.",
                "type": "string"
              },
              "env": {
                "description": "Docker image reference for the test container.",
                "type": "string"
              }
            }
          }
        },
        "macos_matrix": {
          "description": "macOS runners to run unit tests on natively, sourced from test/environments.yaml.",
          "type": "array",
          "items": {
            "description": "A single macOS unit test runner.",
            "type": "object",
            "additionalProperties": false,
            "required": [
              "runner",
              "clean_path"
            ],
            "properties": {
              "runner": {
                "description": "GHA runs-on label for the macOS runner.",
                "type": "string"
              },
              "clean_path": {
                "description": "Whether to strip GHA pre-installed tools from PATH before running tests.",
                "type": "boolean"
              }
            }
          }
        }
      }
    },
    "cd": {
      "description": "Config slice for the cd reusable workflow (GHCR publish, GitHub Release, GitHub Pages deploy).",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "enabled",
        "features",
        "artifact_src_name",
        "artifact_src_path",
        "artifact_dist_name",
        "artifact_dist_path",
        "features_src_path",
        "registry",
        "git_bot_name",
        "git_bot_email",
        "pages_environment"
      ],
      "properties": {
        "enabled": {
          "description": "Whether to run the CD pipeline; true only when at least one feature has an untagged version on main.",
          "type": "boolean"
        },
        "features": {
          "description": "Features to release, each with its version string and the git tag to create.",
          "type": "array",
          "items": {
            "description": "A single feature release entry.",
            "type": "object",
            "additionalProperties": false,
            "required": [
              "feature",
              "version",
              "tag"
            ],
            "properties": {
              "feature": {
                "description": "Feature ID, e.g. install-git.",
                "type": "string"
              },
              "version": {
                "description": "Version string from metadata.yaml, e.g. 1.2.3.",
                "type": "string"
              },
              "tag": {
                "description": "Annotated git tag to create, e.g. install-git/1.2.3.",
                "type": "string"
              }
            }
          }
        },
        "artifact_src_name": {
          "description": "Name of the source artifact downloaded for GHCR publish.",
          "type": "string"
        },
        "artifact_src_path": {
          "description": "Local path where the source artifact is extracted.",
          "type": "string"
        },
        "artifact_dist_name": {
          "description": "Name of the distribution artifact downloaded for GitHub Release upload.",
          "type": "string"
        },
        "artifact_dist_path": {
          "description": "Local path where the distribution artifact is extracted.",
          "type": "string"
        },
        "features_src_path": {
          "description": "Path passed to devcontainers/action base-path-to-features for GHCR publish.",
          "type": "string"
        },
        "registry": {
          "description": "Container registry hostname used for docker/login-action during GHCR publish.",
          "type": "string"
        },
        "git_bot_name": {
          "description": "Git user.name for annotated tag commits.",
          "type": "string"
        },
        "git_bot_email": {
          "description": "Git user.email for annotated tag commits.",
          "type": "string"
        },
        "pages_environment": {
          "description": "GitHub Actions environment name used by the GitHub Pages deployment job.",
          "type": "string"
        }
      }
    }
  },
  "$defs": {
    "artifact": {
      "description": "A GitHub Actions artifact: its name, the local path it maps to, and how long to retain it.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "path",
        "retention_days"
      ],
      "properties": {
        "name": {
          "description": "Artifact name used with actions/upload-artifact and actions/download-artifact.",
          "type": "string"
        },
        "path": {
          "description": "Local filesystem path the artifact is uploaded from or downloaded to.",
          "type": "string"
        },
        "retention_days": {
          "description": "Number of days before GitHub automatically deletes the artifact.",
          "type": "integer"
        }
      }
    }
  }
}
